{
  "name": "sheearns",
  "version": "1.0.0",
  "private": true,
  "description": "Alternative credit readiness for women micro-entrepreneurs in India",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/index.js"
  }
}
